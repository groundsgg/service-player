quarkus.flyway.migrate-at-start=false
quarkus.scheduler.enabled=false

# The @QuarkusTest REST clients don't attach a bearer; disable auth so the
# request filter passes calls through (the JWT path is unit-tested in
# AuthClaimsTest + WorkloadAuthFilterTest, and exercised live in-cluster).
grounds.auth.enabled=false
